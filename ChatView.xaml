<UserControl x:Class="YMM4ChatPlugin.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="600" Width="750">
    <UserControl.Resources>
        <!-- システムメッセージのスタイル -->
        <Style x:Key="SystemMessageStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>
        <!-- 参加/退出通知のスタイル -->
        <Style x:Key="JoinLeaveMessageStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="Foreground" Value="DarkGray"/>
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{Binding DarkModeBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 上部コントロールエリア -->
        <WrapPanel Grid.Row="0" Margin="10" VerticalAlignment="Center">
            <TextBlock Text="ユーザー名:" VerticalAlignment="Center" Margin="0,0,5,0"
                       Foreground="{Binding DarkModeForeground}"/>
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Width="120" Margin="0,0,15,0"/>
            <Button Command="{Binding CreateRoomCommand}" Content="✨ ルームを作成" Width="140" Margin="0,0,10,0"/>
            <TextBox Text="{Binding RoomIdInput, UpdateSourceTrigger=PropertyChanged}" Width="120" ToolTip="ルームIDを入力"/>
            <Button Command="{Binding JoinRoomCommand}" Content="参加" Width="60" Margin="0,0,10,0"/>
            <Button Command="{Binding ToggleDarkModeCommand}" Content="🌓 ダークモード" Width="130"/>
            <Button Command="{Binding ExportHistoryCommand}" Content="📁 エクスポート" Width="110" Margin="10,0,0,0"/>
            <Button Command="{Binding ImportHistoryCommand}" Content="📂 インポート" Width="110"/>
        </WrapPanel>

        <!-- 接続情報表示エリア -->
        <Border Grid.Row="1" Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="1" Margin="10,0,10,10" Padding="5">
            <WrapPanel VerticalAlignment="Center">
                <TextBlock Text="ルームID: " VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding CurrentRoomId}" Width="150" IsReadOnly="True"/>
                <Button Command="{Binding CopyRoomIdCommand}" Content="📋 コピー" Width="70" Margin="10,0,0,0"/>
                <TextBlock Text="  ※ このIDを他の人に共有してください" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="11" Foreground="Gray"/>
            </WrapPanel>
        </Border>

        <!-- メッセージ表示エリア -->
        <ListBox Grid.Row="2" ItemsSource="{Binding Messages}" Margin="10,0,10,10"
                 Background="{Binding DarkModeBackground}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding}">
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding DisplayText}" TextWrapping="Wrap"
                                                       Foreground="{Binding DataContext.DarkModeForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Type}" Value="System">
                                        <Setter Property="ContentTemplate">
                                            <Setter.Value>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding DisplayText}" TextWrapping="Wrap"
                                                               Style="{StaticResource SystemMessageStyle}"/>
                                                </DataTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Type}" Value="JoinLeave">
                                        <Setter Property="ContentTemplate">
                                            <Setter.Value>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding DisplayText}" TextWrapping="Wrap"
                                                               Style="{StaticResource JoinLeaveMessageStyle}"/>
                                                </DataTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- メッセージ送信エリア -->
        <Grid Grid.Row="3" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" Margin="0,0,5,0"
                     MinWidth="100"
                     Background="{Binding DarkModeBackground}" Foreground="{Binding DarkModeForeground}"/>
            <Button Grid.Column="1" Content="送信" Command="{Binding SendMessageCommand}" Width="70" Height="30" IsDefault="True"/>
        </Grid>
    </Grid>
</UserControl>